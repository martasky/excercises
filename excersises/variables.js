"use strict";

const bool = true;
const num = 41;
const str = "Peter";
const obj = {
  cats: 2,
  cars: 1,
};
const nothing = null;
let undf;
const symbol = Symbol("symbol");
console.log(bool);

//you can just type the name in console to see what it is

// or better just use typeof str and it will return the word "string"
